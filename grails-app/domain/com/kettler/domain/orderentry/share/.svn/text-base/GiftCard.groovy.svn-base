package com.kettler.domain.orderentry.share

class GiftCard implements Serializable {
    Date dateCreated
    Date lastUpdated
    String itemNo
    Float originalValue
    Float currentValue
    Integer cardNumber
    Integer orderNumber
    Long consumerId

    static constraints = {
    }

    static mapping = {
        cardNumber column:'card_number', index: 'NumIndex'
    }
}
